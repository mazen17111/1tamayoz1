# تعليمات وتوجيهات منصة التميز التعليمية (قواعد الحفاظ الإلزامي)

> **تنبيه صارم وحاسم لجميع وكلاء ونماذج الذكاء الاصطناعي والمطورين:**
> المنصة الآن في نسختها النهائية المعتمدة والكاملة، والمستخدم راضٍ تماماً عن التصميم والمحتوى والوظائف.
> **يُمنع منعاً باتاً** إعادة تصميم المنصة، أو إعادة بنائها، أو استرجاع أي نسخة سابقة أو قوالب قديمة.

---

### 1. مرجعية النسخة الأساسية (Baseline Rule)
- آخر نسخة محفوظة من المنصة هي النسخة الأساسية والنهائية المعتمدة دائماً.
- يجب الحفاظ التام على جميع التغييرات والمحتويات التي أضافها أو عدلها المستخدم.

### 2. ضمانات الحفظ الدائم
- **الفيديوهات**: جميع الفيديوهات الحالية والمستقبلية، وملفاتها وروابطها وبياناتها تظل محفوظة بشكل دائم ولا يجوز تعديلها أو حذفها إلا بطلب صريح من المستخدم.
- **الصفحات والأقسام**: جميع الأقسام (الكمي واللفظي وأي أقسام إضافية)، والتنسيقات، والأزرار، والأيقونات، والنصوص، والشعارات تظل محفوظة بالكامل.
- **المحتوى الجديد**: أي محتوى جديد يضيفه المستخدم (دروس، مصادر، ملفات PDF، اختبارات، أسئلة) يجب حفظه فوراً وبشكل دائم في قاعدة البيانات `data/db.json` وفي مجلد الرفع `data/uploads/`.
- **التعديلات الجديدة**: أي تعديل يُجريه المستخدم يجب أن يبقى محفوظاً ونافذاً في قاعدة البيانات والواجهة ولا يختفي.
- **التحقق وتأكيد الموقع**: كود التحقق الخاص بمحرك البحث (Google Site Verification) في `index.html` يجب أن يظل ثابتاً ومحفوظاً دائماً دون مساس:
  `<meta name="google-site-verification" content="D5ZT5yqOkOi2t0gcdpxMiNgXnDSRJvfV7gT9guY3Qgk" />`

### 3. قواعد النشر والتحديث (Deployment Rules)
- عند النشر أو إعادة النشر أو عمل Build، يجب نشر أحدث نسخة محفوظة فقط، وعدم استخدام أي كود أو بيانات قديمة.
- ملف `src/defaultData.ts` يجب أن يظل دائماً متطابقاً مع البيانات الأساسية الحالية لحمايتها من أي فقدان في حال حدوث أي استرجاع طارئ.

### 4. المحظورات القطعية (Strict Prohibitions)
- ❌ ممنوع استرجاع نسخة قديمة أو استبدال النسخة الحالية بها.
- ❌ ممنوع حذف أو فقدان أي محتوى أو ملف أو فيديو أو تعديل أضافه المستخدم.
- ❌ ممنوع إعادة المنصة إلى حالتها القديمة أو قوالبها الافتراضية لأي سبب كان.
- ❌ ممنوع إجراء أي عملية قد ينتج عنها مسح أو إعادة تهيئة أو تراجع في البيانات تلقائياً دون التوقف وطلب التأكيد الصريح والمباشر من المستخدم أولاً.
- ❌ لا تقم بتغيير شكل أو تصميم المنصة أو الألوان أو الخطوط أو توزيع العناصر.
